describe.skip("create a status", () => {
    it("renders a column when the plus button is clicked", () => {

    })

    it("cancels the creation if the user clicked away from the column", () => {

    })

    it("cancels the creation if the column name is empty", () => {

    })

    it("append the new column to the end after the user filled the name and pressed the enter key", () => {

    })
})

describe("read a status", () => {
    it("renders all status name and their belonging issues correctly", () => {

    })

    it("shows the dot icon when the status header is hovered", () => {

    })

    it("renders the select menu containing the delete option when the dot icon is clicked", () => {

    })
})

describe("update a status", () => {
    it("update the status name if the user fills the text field and confirms the update", () => {

    })
})

describe("delete a status", () => {
    it("renders a confirmation modal when the user clicks the 'delete' option", () => {

    })

    it("update the state after the user confirms the deletion", () => {

    })

    it("rendered the view excluding the deleted column", () => {

    })
})