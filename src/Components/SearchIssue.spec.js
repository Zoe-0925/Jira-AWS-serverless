describe("The issue search feature", () => {
    it("renders correctly", () => {

    })

    it("renders matching issues in the list view after any matching record is found", () => {

    })

    it("renders the error message if no record is matching", () => {

    })
})