describe("Visibility Filters", () => {
    it("renders the clear filter link when at least 1 filter is enabled", () => {

    })

    it("enables the none filter when the clear filter link is clicked", () => {

    })

    describe("The none filter for the issue list view", () => {
        it("renders all issues within the project, grouped by the status, when the board page is load", () => {

        })
    })

    describe("The epic filter for the issue list view", () => {
        it("renders all child issues of the multiple selected epics when the filter is enabled", () => {

        })
    })

    describe("The epic filter for the issue list view", () => {
        it("renders all issues with the selected label when the filter is clicked", () => {

        })
    })

})

describe("Group By Filters", () => {


})


describe("The modal to update an attribute of an issue", () => {
    it("renders a modal containing a multi-select menu with options of all options provided", () => {

    })

    it("updates a particular attribute of an issue when the user clicks the 'done' button", () => {

    })

    it("cancels the update if the user clicks the 'cancel' button", () => {

    })

    it("disables the 'done' button if no option is selected", () => {

    })
})


describe("The modal to delete an item", () => {
    it("renders correctly", () => {

    })

    it("does not delete the item if the user cancels the deletion", () => {

    })

    it("delete the item if the user confirms the deletion", () => {

    })
})